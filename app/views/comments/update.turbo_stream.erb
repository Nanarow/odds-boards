<%= turbo_stream.update "board-#{@board.id}-comments-form" do %>
  <%= render 'form', comment: Comment.new, board: @board %>
<% end %>
<%= turbo_stream.replace "comment-#{@comment.id}" do %>
  <%= render @comment %>
<% end %>