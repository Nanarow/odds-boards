<% new_comment = @comment.errors.any? ? @comment : Comment.new  %>
<% parent = @parent_id.present? ? Comment.new(id: @parent_id) : @comment %>
<%= turbo_stream.update "comment-#{@comment&.id || @parent_id}-reply-form" do %>
  <%= render 'form', comment: new_comment, board: @comment.board, parent:  %>
<% end %>