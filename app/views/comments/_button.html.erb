<%# locals: (board: Board) %>
<button
  id="comments-button"
  class="flex cursor-pointer items-center gap-2 btn btn-xs rounded-full btn-ghost"
  data-testid="comments-button"
  >
  <%= render_icon 'comment', class: 'size-4' %>
  <%= board.comments.count %> comments
</button>
