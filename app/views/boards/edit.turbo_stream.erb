<%= turbo_stream.update 'board-form' %>
<%= turbo_stream.replace "board-#{@board.id}" do %>
  <div class="flex w-full -mt-4" id="board-<%= @board.id %>">
    <%= render "form", board: @board %>
  </div>
<% end %>
