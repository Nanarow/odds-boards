<div class="flex gap-4 px-32">
  <%= render 'sidebar' %>
  <section class="main-content w-3/4 flex flex-col gap-4">
    <%= render 'menubar' %>
    <div class="flex flex-col gap-4">
      <%= yield %>
    </div>
  </section>
</div>
