<div class="flex gap-4 container justify-center">
  <%= render 'sidebar' %>
  <div class="w-full sm:w-3/4 flex flex-col gap-4">
    <%= render 'menubar' %>
    <%= turbo_frame_tag 'board-form-section',class:'empty:-my-2' %>
    <section class="flex flex-col gap-4">
      <%= yield %>
    </section>
  </div>
</div>
