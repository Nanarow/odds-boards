<%# locals: (board:)  %>
<!--<div data-controller="dropdown" class="relative" >
  <button 
    type="button" 
    data-action="propagation#stop dropdown#toggle click@window->dropdown#hide"
    class="btn btn-ghost btn-square btn-xs">
    <%#= render_icon 'ellipsis-vertical', class: 'size-4' %>-->
<!--  </button>
  <div
    data-dropdown-target="menu"
    class="hidden transition transform origin-top-right absolute right-0"
    data-transition-enter-from="opacity-0 scale-95"
    data-transition-enter-to="opacity-100 scale-100"
    data-transition-leave-from="opacity-100 scale-100"
    data-transition-leave-to="opacity-0 scale-95"
  >
    <div class="menu bg-base-200 rounded-box w-56 p-0">
      <li>
        <div>
          <%#= render_icon 'pen-line', class: 'size-4' %>-->
<%#= button_to load_edit_board_path(board),
            data: { 
              turbo: true, 
              testid: "edit-board-#{board.id}-button",
              action: 'propagation#stop' 
            } do %>
  <!--            Edit-->
  <%# end %>
  <!--        </div>
      </li>
    </div>
  </div>
</div>-->
  <div class="flex gap-2 items-center">
    <%= button_to load_edit_board_path(board), 
            class: 'btn btn-secondary btn-xs ', 
            data: { 
              turbo: true, 
              testid: "edit-board-#{board.id}-button",
              action: 'propagation#stop' 
            } do %>
      <%= render_icon 'pen-line', class: 'size-4' %>
      Edit
    <% end %>
    <%= button_to confirm_delete_board_path(board), 
            class: 'btn btn-error btn-xs',
            data: { 
              turbo: true, 
              testid: "confirm-delete-board-#{board.id}-button",
              action: 'propagation#stop'
            } do %>
      <%= render_icon 'trash', class: 'size-4' %>
      Delete
    <% end %>
  </div>